## 文件结构

|文件|作用|
| :--- | :--- |
|📂options|插件的配置页面|
|📂popup|点击浏览器右上角插件图标后，弹出的那个小窗口的界面|
|📄background.js|插件的常驻后台。全局唯一的。无论开了多少个网页，后台脚本只有一个在运行。<br />看不见网页内容。它无法直接使用 document 或 window 对象|
|📄content.js|注入到网页里的脚本。<br />不能直接调用大部分 chrome.* 接口（比如不能直接管理下载、不能直接修改浏览器设置）|
